# from_sqltest MANIFEST — scope ledger

Every `.sqltest` file considered for porting gets a row. Statuses:
`ported` · `partial` (note says what is missing) · `skipped` (note says why) ·
`out-of-scope` (feature pyturso will not implement — cite PLAN.md).

| Source (sqlite/conformance/sqlite-sqltests/) | Status | Corpus file | Notes |
|---|---|---|---|
| _(none yet — first entries land with Phase 5)_ | | | |
