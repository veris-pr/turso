# HOWTO — corpus/phase10_surface

Breadth cases: GROUP BY/HAVING/aggregates, DISTINCT, compound SELECTs,
subqueries, UPDATE/DELETE with indexes, UPSERT, views, PRAGMA subset —
one directory-per-feature is fine as this grows.
